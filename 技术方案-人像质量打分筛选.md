# 技术方案 - 人像质量打分筛选

> 目标：在用户上传的几十张至上百张照片中，自动挑选出质量较好的照片（以人像综合打分为主），供后续分类、精修、文案生成流程使用。
>
> 关联模块：`backend/app/ai/screener.py`（当前为 `MockScreener`）、`backend/app/ai/pipeline.py`（`_screen` 阶段）、`backend/app/ai/base.py`（`ScreenerProvider` / `SelectedPhoto`）。

---

## 一、结论先行（TL;DR）

- **纯大模型能做，但不建议只靠它**。多模态大模型（qwen-vl-plus / GPT-4o / Claude）确实能理解构图、表情、意境，但它输出的"质量分"是相对、主观的，缺乏统一标度，同一张图多次打分抖动大，且 100+ 张全量跑成本高、延时大，对"轻微模糊 / 噪点"等像素级缺陷反而不敏感。
- **推荐：分层混合方案**——先用便宜、确定、可批量跑的「技术质量 + 人脸质量」把明显差的淘汰，再用已接入的 `qwen-vl-plus` 只对**筛后候选**做语义精排。成本可控、效果稳定，与现有技术栈契合度最高。

---

## 二、问题拆解：什么叫"质量好的人像照"

可拆为三层，缺一不可：

1. **技术质量**：清晰度（无糊）、曝光正常、构图合理、无严重噪点 / 畸变。
2. **人像质量（核心）**：人脸清晰、眼睛睁开、表情自然、姿态端正、无遮挡、单 / 多人判定。
3. **美学 / 语义**：是否有"值得保留的纪念感"——背景好看、瞬间抓拍到位、不是废片。

本场景偏 2 + 3，且规模为几十~上百张，因此方案必须**能批量、低成本、低延时**。

---

## 三、可选方案对比

| 方案 | 能力 | 成本 / 100 张 | 延时 | 缺点 | 适配度 |
|---|---|---|---|---|---|
| **A. 纯 VLM 打分**（qwen-vl-plus 等） | 懂构图 / 表情 / 意境 | 高（图 token 贵，约 ¥几~十几元，含重打） | 高（并发受限，分钟~十分钟级） | 分数无统一标度、抖动大、对轻微模糊不敏感、难批量 | 中（只适合精排） |
| **B. 专用 IQA 模型**（NIMA / MUSIQ / MANIQA / CLIP-IQA） | 输出可比较的美学 / 技术分，稳定 | 极低（本地推理） | 低 | 需自部署 GPU / 模型；不懂"闭眼 / 表情"等语义 | 中（自建成本） |
| **C. 商业 IQA API**（阿里云视觉智能开放平台） | 清晰度 / 曝光 / 构图评分，开箱即用 | 极低（约 ¥0.5 内） | 低 | 非人像专用语义判断 | **高（同生态）** |
| **D. 人脸质量 API**（人脸检测 + 质量分） | 脸数 / 模糊 / 姿态 / 眼闭 / 遮挡 / 颜值分 | 低（约 ¥1 内） | 低 | 只覆盖人像，不评风景 | **高（同生态）** |
| **E. 传统 CV 启发式**（拉普拉斯方差 / 直方图 / 感知哈希） | 去糊、去曝光异常、去重 | 近乎 0 | 最低 | 不懂美学与语义，只能判缺陷 | **高（兜底去重）** |

**关键判断**：100+ 张全部用 VLM 打分，成本和延时都不可接受，且 VLM 在"轻微模糊 / 噪点"这类像素级缺陷上反而弱于专用模型。VLM 的真正价值在**语义层"这张值不值得留"**，应放在末段只对候选打分。

---

## 四、推荐架构（贴合现有技术栈）

```
L0  去重 / 废片剔除    传统 CV（感知哈希 / 拉普拉斯方差）        几乎免费, 全量
        ↓
L1  技术质量分        阿里云 IQA API（清晰度 / 曝光 / 构图美学）   便宜, 全量 100 张
        ↓
L2  人像质量分        人脸检测 API（模糊 / 姿态 / 眼闭 / 遮挡 / 脸数） 便宜, 有人脸的图
        ↓
L3  语义精排          qwen-vl-plus  仅对 L1+L2 筛后的 Top-K 候选     控制成本
        ↓
   综合加权 → quality_score → 分组精选 max_per_group
```

**为什么这样分层**：L0 / L1 / L2 都是确定、便宜、可并发全量跑的能力（项目已在阿里云生态，COS + DashScope 同源，集成成本低）；只有 L3 用 VLM，且只对淘汰明显差片后的候选（约 20–30 张）打分，成本直接砍掉 70%+。

**综合打分示例**（权重可调）：

```
quality_score = w1*清晰度 + w2*曝光 + w3*构图 + w4*人脸质量
                 (+ w5*VLM 语义分  仅候选)     # 人像权重 w4 调高，风景走 w1~w3
```

人像照与风景照使用两套权重（与现有 `PhotoType: portrait / landscape` 对应）。

---

## 五、与现有代码的衔接（改动很小）

- `ScreenerProvider.screen()` 接口已就绪，**只需把 `MockScreener` 换成 `RealScreener`**，`pipeline.py` 中 `await self._screen(...)` 调用不动。
- `SelectedPhoto` 已有 `quality_score`、`face_count`；扩展加 `sharpness / exposure / composition / face_quality` 子分即可，`_to_dict` 一并回传前端用于排序展示。
- 复用 `photo_classifier.classify_photos` 的并发范式（`asyncio.gather` + 异常兜底）来并发调 IQA / 人脸 API。
- L3 可复用同一个 `qwen-vl-plus` client（已有 `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL`），仅换 prompt 要求输出 0–1 分 + 简短理由。

### 建议新增配置（`backend/app/core/config.py`）

```
# ---------- 图像质量评估 IQA ----------
IQA_PROVIDER: str = "aliyun"          # aliyun / local / none
IQA_ACCESS_KEY_ID: str = ""
IQA_ACCESS_KEY_SECRET: str = ""
IQA_REGION: str = "cn-shanghai"

# ---------- 打分权重（人像 / 风景两套）----------
SCORE_WEIGHTS_PORTRAIT: str = "sharpness:0.2,exposure:0.15,composition:0.15,face:0.5"
SCORE_WEIGHTS_LANDSCAPE: str = "sharpness:0.3,exposure:0.3,composition:0.4,face:0.0"

# L3 语义精排候选数上限（控制 VLM 成本）
SEMANTIC_RERANK_TOPK: int = 30
```

---

## 六、落地路线图（建议顺序）

1. **Phase 1（最快见效，纯 API）**：接入阿里云「图像清晰度评分 + 图像曝光度评分 + 图像构图美学评分」+ 人脸检测质量分，跑通 L0–L2，替换 Mock。此阶段不依赖 `LLM_API_KEY` 也能工作。
2. **Phase 2**：L0 去重（感知哈希 / 相似帧合并），避免精修重复片。
3. **Phase 3（可选增强）**：L3 用 qwen-vl-plus 对候选做语义精排，提升"精选感"。
4. **Phase 4**：用少量人工标注的"好 / 差"样本回归校准权重。

---

## 七、关键风险与注意事项

- **VLM 打分一致性**：`temperature=0` + 固定 prompt + 要求结构化 JSON 输出；不要对全量打分（成本 + 抖动双输）。
- **人像 vs 风景两套权重**：现有 `type` 字段已区分，打分逻辑要分流处理。
- **成本护栏**：VLM 仅对 L1 + L2 后的候选打分；IQA / 人脸 API 也设并发上限，避免 100 张瞬时打爆配额。
- **失败兜底**：任何一层 API 异常 → 该子分置中值（如 0.7），不影响主流程（与现有 `photo_classifier` 失败兜底思路一致）。
- **隐私合规**：人脸质量检测涉及人脸数据，需在用户协议 / 隐私政策中声明用途，并确保数据仅用于本次处理、及时清理。

---

## 八、附：候选技术清单

| 层级 | 首选（同生态） | 备选（自建 / 开源） |
|---|---|---|
| L0 去重 | `imagehash`（pHash / dHash） | OpenCV 感知哈希 |
| L0 去糊 | OpenCV 拉普拉斯方差 | — |
| L1 技术质量 | 阿里云视觉智能开放平台：清晰度 / 曝光 / 构图美学评分 | NIMA / MUSIQ / CLIP-IQA（`IQA-PyTorch`） |
| L2 人脸质量 | 阿里云人脸检测（模糊 / 姿态 / 眼闭 / 遮挡 / 质量分） | InsightFace + 自定义质量头 |
| L3 语义精排 | qwen-vl-plus（已接入 DashScope） | GPT-4o / Claude（成本更高） |

---

_文档版本：v1.0ㅤ｜ㅤ编写日期：2026-07-14_
